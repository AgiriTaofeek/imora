# Workers

> Status: Deferred at the service-implementation level, but not undocumented — this is the other container with full internal component detail already specified in [component-diagrams.md](../04-architecture/component-diagrams.md) (`LegalHoldChecker`, `SelectivePurgeExecutor`, `EvidenceExportGenerator`, `DeletionExecutor`), plus the CronJob/Deployment split in [kubernetes.md](../09-infrastructure/kubernetes.md). Reserved for implementation-level detail (actual code structure) once real code exists, per [docs/README.md](../README.md)'s folder table.

Background workers that process, enrich, and route ingested events.

## Overview

_TBD_
